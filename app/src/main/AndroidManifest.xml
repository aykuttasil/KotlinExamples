<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aykuttasil.kotlinexamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KotlinPattern.KotlinDecorator" />
        <activity android:name=".KotlinPattern.KotlinSingleton" />
        <activity android:name=".KotlinPattern.KotlinTemplate" />
        <activity android:name=".KotlinPattern.KotlinStrategy" />
        <activity android:name=".KotlinExtension" />
        <activity android:name=".KotlinIterator" />
        <activity android:name=".KotlinLambda" />
    </application>

</manifest>